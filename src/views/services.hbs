{{! Extends the main layout }}
{{> header }}

<div class="min-h-screen flex flex-col items-center justify-center text-center p-6">
    <h1 class="text-4xl font-bold text-blue-600">{{heroText}}</h1>
    <p class="text-lg mt-4 text-gray-700">Welcome to our simple Handlebars & TailwindCSS landing page!</p>
    <div>
        Services Page
    </div>
</div>

{{> footer }}
